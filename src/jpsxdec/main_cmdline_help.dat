Usage:

java -jar jpsxdec.jar { -? | -h | -help}
  Display this help

java -jar jpsxdec.jar -f <in_file> -x <index_file>
  Build an index of <in_file> and save it as <index_file>

java -jar [ -x <index_file> ] [ -f <in_file> ] <main_command_and_options>
  Main commands that require an index file.
  Use an existing <index_file> (optionally overriding the <in_file> listed in it)
  or generate an index on the fly for <in_file> (optionally saving it as <index_file>)
  and perform one of the following <main_command_and_options>

    -i/-item # <item_command_and_options>
    -a/-all <video, audio, file> <item_command_and_options>
      Perform <item_command_and_options> on one index item,
      or all index items of a type

        If no other command is specified:
          Saves (after possibly converting) index item #
          using optional processing options (see item's help for options)

        -?/-h/-help
          Display help about the index item

        (for audio/video)
        -play
          Show real-time player for index item

        (see manual or item's help for full list of possible commands)

    -visualize <pdf_out_file>
      Display the layout of sectors and index items {disabled}

java -jar -f <in_file> <main_command_and_options>
  Main commands that only require only an input file

    -static <type>
      Process special static file types {disabled}

    -copysect { # | #-# }
      Copy sectors to a another file.

    -sectordump <out_file>
      Write list of sector types to <out_file> (for debugging)

  Additional option (optional):
    -v/-verbose #
    How much info to print: 0 for none, 1 for only errors,
                            2 for normal, 3 for extra
